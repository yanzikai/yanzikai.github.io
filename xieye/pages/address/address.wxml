<!--pages/address/address.wxml-->
<scroll-view scroll-y class='viewbox'>
  <view class='vb'>
    <block wx:for="{{address}}" wx:key="index">
      <view data-add="{{item}}" class="{{ item==current ? 'active':'' }}" bindtap='select' >{{item}}</view>
    </block>
  </view>
</scroll-view>